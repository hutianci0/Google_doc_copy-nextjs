import { FullScreenLoader } from '@/app/components/full-screen-loader'

//handle "awaiting" in the documentID folder
const LoadingPage = () => {
  return <FullScreenLoader label="documen loading...." />
}

export default LoadingPage
